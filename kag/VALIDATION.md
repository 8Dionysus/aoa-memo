# Local KAG Provider Validation

On-demand procedure for `kag/AGENTS.md`.

## Owner and source checks

Run the memo-owned source lane:

This surface owns no distinct executable procedure; use the [parent validation route](../VALIDATION.md).

Then run the pinned `aoa-kag` provider-family route named by
`kag/manifest.json`. The sibling owner check is separate evidence; do not
replace it with inspection of generated indexes or a stale local checkout.
