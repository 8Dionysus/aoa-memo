"""Implementation module registry for the validate_memo compatibility CLI."""

from __future__ import annotations

from . import (
    _shared,
    _shared_datetime,
    _shared_io,
    _shared_paths,
    _shared_quest_constants,
    _shared_refs,
    _shared_schema,
    _shared_schema_constants,
    eval_boundary,
    eval_boundary_helpers,
    eval_boundary_pack,
    eval_boundary_pilot,
    eval_boundary_wider,
    handoff_boundary,
    handoff_boundary_bridge,
    handoff_boundary_kag,
    handoff_boundary_playbook,
    handoff_boundary_routing,
    memory_context,
    memory_context_contracts,
    memory_context_profiles,
    memory_context_readiness,
    memory_context_registry,
    questbook,
    questbook_discovery,
    questbook_external,
    questbook_projection,
    questbook_surface,
    runtime_boundary,
    runtime_receipt_evidence,
    runtime_receipt_support,
    runtime_receipts,
    runtime_writeback,
    runtime_writeback_builders,
    runtime_writeback_governance,
    runtime_writeback_growth,
    runtime_writeback_targets,
    schema,
    schema_recall_contracts,
    schema_surfaces,
)


PROFILE_MODULES = (
    _shared,
    _shared_paths,
    _shared_quest_constants,
    _shared_schema_constants,
    _shared_io,
    _shared_datetime,
    _shared_refs,
    _shared_schema,
    schema,
    questbook,
    memory_context,
    runtime_writeback,
    runtime_receipts,
    runtime_boundary,
    handoff_boundary,
    eval_boundary,
    handoff_boundary_routing,
    handoff_boundary_playbook,
    handoff_boundary_bridge,
    handoff_boundary_kag,
    eval_boundary_helpers,
    eval_boundary_pilot,
    eval_boundary_wider,
    eval_boundary_pack,
    questbook_discovery,
    questbook_external,
    questbook_projection,
    questbook_surface,
    memory_context_profiles,
    memory_context_readiness,
    memory_context_registry,
    memory_context_contracts,
    runtime_writeback_builders,
    runtime_writeback_targets,
    runtime_writeback_governance,
    runtime_writeback_growth,
    runtime_receipt_support,
    runtime_receipt_evidence,
    schema_surfaces,
    schema_recall_contracts,
)
