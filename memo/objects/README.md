# Reviewed Objects

This directory holds reviewed memory object bundles.

Each object lives at:

```text
objects/<kind-dir>/<year>/<slug>/
  object.json
  MEMO.md
```

Use `../OBJECT_SHAPE.md` for the exact storage contract.
