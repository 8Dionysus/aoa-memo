# Corpus Support

This directory holds support surfaces that serve the reviewed memory corpus.

- `provenance-threads/` is for reviewed thread material that connects multiple
  memory objects.
- `recall-contracts/` is for reviewed recall contracts that directly govern the
  corpus.

Examples and teaching fixtures stay in `examples/`; generated read models stay
in `generated/`.
