# Titan Remembrance Source Ref Policy

Mneme may remember only as candidate memory unless owner review promotes the record.

A Titan memory record must include at least one provenance anchor: `source_refs[]`, `receipt_id`, `session_id`, or `lineage_event_ref`.

Recall result wording: `candidate memory; verify via source refs`.
