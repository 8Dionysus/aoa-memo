# AGENTS.md

## Applies to

`mechanics/checkpoint/legacy/` and historical material below it.

## Role

`legacy/` preserves checkpoint placement provenance.

It is not the active route for checkpoint work and must not receive new active
schemas, examples, generated outputs, scripts, or tests.

## Local delta

This district records placement history only. The active `checkpoint` route remains
in `../docs/`; the package `PROVENANCE.md` and `OWNER_MAP.md` retain current
ownership and stronger-owner boundaries. The shared legacy hierarchy is
inherited from `mechanics/AGENTS.md`.

## Boundaries

- Keep this lane about former paths and provenance.
- Do not duplicate active checkpoint docs here.
- Do not cite legacy paths as source truth.
- Route active checkpoint work to `mechanics/checkpoint/`.

## Verification

Use `legacy/INDEX.md` for the narrow historical lookup and the nearest
`VALIDATION.md` route when a provenance edit is actually required.
