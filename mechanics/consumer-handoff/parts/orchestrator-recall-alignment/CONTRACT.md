# Orchestrator recall alignment Contract

## Owner Boundary

`aoa-memo` owns this part only as a bounded memory-layer operation inside `mechanics/consumer-handoff/`.

Stronger claims route through `../../OWNER_MAP.md`. Source placement and legacy context route through `../../PROVENANCE.md` and historical placement is recorded in `../../PROVENANCE.md`.

## Source Surfaces

- [ORCHESTRATOR_MEMORY_ALIGNMENT](../../docs/ORCHESTRATOR_MEMORY_ALIGNMENT.md)

## Contract

aligns router, review, and bounded-execution quest families to memo recall posture

For `codex_owner_orientation_v0`, `aoa-memo` accepts only the exactly pinned
`codex_owner_orientation_plan_v0` schema from `aoa-sdk`. The plan must retain
the exact consumer profile and C11 policy, D0/R1 explicit pull, current C18
and C19 refs, confirmed/current reviewed-corpus cards, visible lifecycle
conflicts and omissions, exact read-model digests, and effect/action ceilings
of `none`/`forbidden`.

The memo packet builder owns the semantic conversion into C08 and C09.
`abyss-stack` owns only C20 delivery. Neither consumer may silently produce,
rerank, persist, promote, or mutate memory.

`codex_owner_orientation_shadow_v0` is a distinct D0/R4 control path. It may
construct C08/C09 for measurement only after SDK, currentness, outcome, erase,
and host-admission checks. Its packets remain consumer-invisible and
delivery-ineligible. Candidate intake is ephemeral C01/C02/C03 workspace
material; metabolism may produce proposal-only dispositions, never durable
candidate writes, C06 transitions, or accepted C11 changes.

`codex_owner_orientation_canary_v0` is separate from both earlier consumers.
Its exact SDK compatibility pin admits only
`codex_owner_orientation_canary_release_plan_v0`, the memo-owned profile, and
the operator-approved C11 policy. A release must bind a frozen selective
shadow plan and bundle, one owner-local tenant, D0/R2, one eval-owned
assignment, one always-shadow counterfactual, current stats outcome refs,
current C18/C19 admission, and at most one item. Secret detection, stale
currentness, unavailable eval, host softening/deferral/denial, exhausted
window, cooldown, or kill switch resolves to explicit non-delivery.

The canary builder owns the C08/C09 observation semantics. `abyss-stack` may
return the exact observation and refs-only C20 evidence, but it may not
reselect, rerank, persist, direct, act, or infer policy. The rollback target is
the unchanged explicit-pull consumer `codex_owner_orientation_v0`.

Outcome-qualified episodic utility is a separate proposal-only surface under
`AOA-MEM-D-0078`. It requires a valid descriptive stats aggregate and an
`aoa-evals` verdict over outcome, action change, counterfactual, delayed
effects, accidental success, and reward hacking. Access count is absent from
the ABI. Pending or overdue delayed outcomes freeze positive adjustment;
terminal success without a material action change carries no qualified
positive utility.

The proposal may change only a bounded candidate for ranking weight, cooldown,
projection choice, abstraction level, cadence, or budget. Rare safety,
constitutional, privacy, or audit episodes keep a preservation floor and may
prefer a source-first projection even when observed frequency is low. The
proposal cannot promote, delete, retract, change owner, expand tenant or
permissions, approve itself, or modify the episode semantic digest. It pins
the exact prior policy version and rollback target, while application remains
outside this owner and Phase 9.

Under `AOA-MEM-D-0081`, agent-local episodic or procedural memory enters this
part only as a content-minimized reviewed-promotion nomination. The source
namespace, generation, agent, tenant, contract digest, local case digest,
outcome refs, duplicate checks, and conflict checks are exact. Cross-tenant
promotion, raw local payload, and direct shared writes fail closed.

The admission receipt may create a proposed C02-compatible memo candidate only
after an operator `approve` or `narrow` decision and only when the material is
neither duplicate nor in unresolved conflict. Duplicate material produces
`duplicate_no_write`; unresolved conflict produces `conflict_quarantine`;
reject and defer remain explicit. Every result leaves the shared ledger
unchanged and performs no C06 semantic transition.

Under `AOA-MEM-D-0083`, the H0 participation hook is observation only. It may
observe `SessionStart`, `UserPromptSubmit`, `PostToolUse` for the `aoa_memo`
MCP family, `PreCompact`, `PostCompact`, `Stop`, and `SessionEnd`. Persisted
fields are limited to allowlisted lifecycle states, coarse workspace/model
classes, prompt-length bucket, route/opportunity class, hashed correlation
refs, memo-tool class, result state, and a receipt hash chain.

The hook must discard prompt text, transcript paths and contents, cwd text,
tool input, tool response, memory content, last assistant message, and
exception text before persistence. It returns no stdout in hook mode, exits
fail-open, cannot block or continue, performs no model call, and holds no
write, semantic, policy, routing, role, proof, or effect authority. An
opportunity or completed MCP result leaves `noticed`, `used_or_rejected`,
`action_change`, and `outcome` exactly `unknown`.

`aoa-session-memory` remains a separate standalone repository and hook owner.
Stable evidence refs from it may later join reviewed analysis, but this
fragment neither imports its code nor requires its runtime. Native Codex hook
composition, binding, deployment, backup, and rollback belong to
`abyss-stack`; this part owns only memo semantics and the fragment source.

H0 observation storage must not become permanent shadow memory. Retention is a
separate operator command, never an implicit hook effect. It plans by default,
preserves at least the 30-day observation horizon, and may erase only a whole
receipt chain that revalidates, ends in `SessionEnd`, and is older than the
selected cutoff. Execution requires explicit acknowledgement, coordinates
against concurrent appenders, removes the matching per-session lock, and
reports only aggregate counts. It cannot erase an open, recent, unreadable, or
invalid chain and has no authority over semantic memory or
`aoa-session-memory`.

## Stop-lines

This part inherits the package stop-lines from `../../README.md#must-not-claim` and `../../OWNER_MAP.md`.

It must not become proof, runtime execution, route dispatch, role authority,
KAG substrate truth, playbook choreography, stats truth, ToS canon, source-owner
acceptance, or private memory unless a stronger owner accepts that work in its
own repository.

This pack additionally fails closed on raw `.aoa`, private ledgers, proactive
delivery, hidden SDK fallback, `aoa-routing` dependence, missing source or
host pins, unknown versions, content-digest drift, and an intent that outlives
its anchor.

Only the exact Phase 8 canary is an exception to the general proactive-delivery
stop-line, and only inside the source-local mechanism lab authorized by
`AOA-MEM-D-0077`. It does not authorize deployment, MCP exposure, a Codex hook,
policy promotion, or landing.

The Phase 9 utility contour is also source-local and lab-only. It does not
authorize an SDK policy admission, runtime ranking change, KAG recall
eligibility, live consumer, deployment, or landing.

The Phase 12 agent-local contour is likewise source-local and lab-only. A
promotion admission receipt is not a reviewed shared memory object, runtime
deployment, proof verdict, or landing receipt.

The participation hook is also source-local and shadow-only. Its fragment is
not a trusted Codex definition, installed runtime, selective cue, proactive
packet delivery, proof of skill selection, proof of use, outcome evidence, or
benefit claim. A visible `UserPromptSubmit` cue or any memory content injection
requires a later operator-versioned policy and separate evidence gate.
