# Writeback Legacy

This legacy route preserves the old flat docs-root placement for writeback
surfaces.

Start from active writeback surfaces before using this route. Legacy material
is for placement audit only.
