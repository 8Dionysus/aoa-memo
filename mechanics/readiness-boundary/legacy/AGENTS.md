# AGENTS.md

## Applies to

`mechanics/readiness-boundary/legacy/` and historical material below it.

## Role

`legacy/` preserves readiness-boundary placement provenance.

It is not the active route for readiness-boundary work and must not receive
new active schemas, examples, generated outputs, scripts, or tests.

## Local delta

This district records placement history only. The active `readiness-boundary` route remains
in `../docs/`; the package `PROVENANCE.md` and `OWNER_MAP.md` retain current
ownership and stronger-owner boundaries. The shared legacy hierarchy is
inherited from `mechanics/AGENTS.md`.

## Boundaries

- Keep this lane about former paths and provenance.
- Do not duplicate active readiness-boundary docs here.
- Do not cite legacy paths as source truth.
- Route active readiness-boundary work to `mechanics/readiness-boundary/`.

## Verification

Use `legacy/INDEX.md` for the narrow historical lookup and the nearest
`VALIDATION.md` route when a provenance edit is actually required.
