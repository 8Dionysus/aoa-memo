interface:
  display_name: "AoA Memo"
  short_description: "Recall, review, or evolve existing AoA memory"
  default_prompt: "Use $aoa-memo to recall, review, or evolve this existing memory object or corpus question against its owner sources, temporal posture, and authority limits."

policy:
  allow_implicit_invocation: true
