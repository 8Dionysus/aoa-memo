# Owner Skill Validation

On-demand procedure for `skills/AGENTS.md`.

## Portable source shape

```bash
skills-ref validate skills/aoa-memo
```

Manual isolated, negative, held-out, coexistence, and bounded-write tasks
decide usefulness. The pinned `aoa-skills` composite action checks owner,
manifest, source, and package digest; the OS installer separately owns
installed byte and mode parity. Green output makes no outcome claim.
