# Example local packets

This directory is intentionally empty in the example port.

Use it for repo-local memo packets that should stay local until a reviewed route
exists. Candidate, receipt, and export packets use their sibling directories.
