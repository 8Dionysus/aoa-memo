# Memo Quest Lane

This lane holds `AOA-MEM-Q-*.yaml` memory-layer obligations.

The lifecycle state in each source file must match its directory. Generated
quest catalog and dispatch surfaces are built from these YAML files.

Use this lane for memo-owned recall, writeback, recurrence, adoption,
checkpoint, and consumer-handoff obligations. Route proof, runtime, playbook,
role, KAG, routing, and source-owner acceptance elsewhere.
