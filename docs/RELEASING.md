# Releasing `aoa-memo`

This is a thin compatibility entrypoint for workspace release tooling that
expects `docs/RELEASING.md`.

The active release procedure lives in [root/RELEASING](root/RELEASING.md).
Do not add independent release policy here.
