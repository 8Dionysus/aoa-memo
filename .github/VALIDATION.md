# GitHub Surface Validation

On-demand procedure for `.github/AGENTS.md`.

## Repository workflow parity

The local release composition is the executable counterpart of the
`Repo Validation` workflow:

This surface owns no distinct executable procedure; use the [parent validation route](../VALIDATION.md).

Inspect workflow-only changes as source as well. A local green result does not
prove the hosted workflow, review, or merge state.
