# Intake

Pre-bundle memory eval pressure lives here as `*.eval_need.json`.

An intake packet may point to guardrail packs, local fixtures, or trace cues,
but it is not a verdict, scoring surface, regression claim, or proof doctrine.
Those routes stay with `aoa-evals`.
